# موقع شخصي لطبيب (Doctor Portfolio Website)

موقع تعريفي شخصي لطبيب بتصميم عصري وحديث (Modern UI/UX)، تم بناؤه باستخدام HTML, CSS, JavaScript فقط بدون أي أطر عمل (Frameworks).

## 📂 هيكلية الملفات (Folder Structure)

```
doctor_portfolio/
├── index.html          # الصفحة الرئيسية (تحتوي على جميع الأقسام)
├── css/
│   └── modern.css      # ملف التنسيقات (تصميم عصري، Glassmorphism، Responsive)
├── js/
│   └── main.js         # ملف التفاعل (القائمة، التحريكات، النموذج)
├── assets/             # مجلد الصور (فارغ حالياً، مجهز لإضافة الصور)
└── README.md           # ملف التعليمات
```

## 🚀 طريقة التشغيل

1. قم بفك الضغط عن المجلد (إذا كان مضغوطاً).
2. افتح المجلد `doctor_portfolio`.
3. انقر مرتين على ملف `index.html` ليفتح في المتصفح المفضل لديك (Chrome, Edge, Firefox, Safari).
4. لا حاجة لتثبيت أي برامج أو خوادم (Server)، الموقع يعمل مباشرة.

## ✨ المميزات

- **تصميم متجاوب (Responsive)**: يعمل بامتياز على الهواتف، الأجهزة اللوحية، والحواسيب.
- **واجهة عصرية (Modern UI)**: استخدام ألوان طبية هادئة، ظلال ناعمة، وتأثيرات زجاجية (Glassmorphism).
- **تفاعلات سلسة**: تنقل ناعم بين الأقسام (Smooth Scrolling) وظهور تدريجي للمحتوى (Scroll Reveal).
- **جاهز للتطوير**: الكود منظم ومعلق عليه (Commented) ليسهل ربطه بـ Backend لاحقاً.

## 🛠️ التعديل على المحتوى

- **النصوص**: افتح `index.html` باستخدام أي محرر نصوص (مثل VS Code أو Notepad) وابحث عن النصوص الموجودة بين الأقواس `[]` لاستبدالها بالبيانات الحقيقية.
- **الصور**: لم يتم استخدام صور خارجية لضمان عمل الموقع فوراً. الصور الحالية هي عبارة عن أشكال CSS و SVG. لإضافة صور حقيقية، ضع الصور في مجلد `assets` وقم بتعديل وسم `<img>` في ملف HTML.
- **الألوان**: يمكنك تغيير ألوان الموقع بالكامل بسهولة من ملف `css/modern.css` عبر تعديل المتغيرات في قسم `:root` في بداية الملف.

---
تم التطوير بناءً على طلبك ليكون بسيطاً، سريعاً، وعالي الجودة.
